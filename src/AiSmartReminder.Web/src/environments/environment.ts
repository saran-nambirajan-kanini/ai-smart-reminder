import { Environment } from './environment.model';

export type { Environment };

export const environment: Environment = {
  production: true,
  apiBaseUrl: 'https://api.aismartreminder.com',
  appName: 'AI Smart Reminder',
};
